version: "3.7"

services:
  apigestiome:
    container_name: apigestiome
    build:
      context: ./api
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - "./api/:/api/"
    ports:
      - 8888:8888