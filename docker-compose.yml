version: "3.7"

services:
  fastapi:
    container_name: fastapi
    build:
      context: ./api
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - "./api/:/api/"
    ports:
      - 8888:8888